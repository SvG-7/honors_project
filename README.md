# honors_project
mplemented the Convex Hull algorithm in C to compute the outer boundary of a set of 2D points. The project supports reading point data from files, handling special edge cases, and generating random datasets for performance analysis.
